# Optional: override the backend URL used by API calls and dev proxy
VITE_API_BASE_URL=http://localhost:8080

# Required session cookie that the client sends with every request
VITE_SESSION_COOKIE=JSESSIONID=YOUR_SESSION_TOKEN
