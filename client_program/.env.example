# Optional: override the default backend URL (http://localhost:8080)
VITE_API_BASE_URL=http://localhost:8080

# Required session cookie that will be sent with every request
VITE_SESSION_COOKIE=JSESSIONID=YOUR_SESSION_TOKEN
