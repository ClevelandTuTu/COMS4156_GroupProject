/**
 * Manager-facing REST controllers for hotel operations.
 * Exposes endpoints to manage reservations and rooms, including listing,
 * detail, updates, status transitions, upgrades, and cancellation.
 * Controllers validate input and delegate business logic to
 * application services, returning standard HTTP responses.
 */
package com.project.airhotel.controller.manager;
